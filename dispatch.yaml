dispatch:
  # Default module serves simple hostname request.
  - url: 'ldrv-project.appspot.com/'
    module: default

  # Send all mobile traffic to the mobile frontend.
  - url: 'socket.ldrv-project.appspot.com/'
    module: socket
